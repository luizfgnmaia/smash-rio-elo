---
title: "Sets"
output: html_notebook
---

```{r}
source("json_to_df.R")
```

```{r}
sets = list.files("tournaments") %>%
  lapply(json_to_df) %>%
  do.call(rbind, .) %>%
  filter(!is.na(score1))

sets
```

A ordem não tá certa, tô jogando contra o Revo depois do João.

```{r}
save(sets, file = "sets.RData")
```

```{r}
write.table(sets, file = "sets.csv", sep = ";", na = "")
```
